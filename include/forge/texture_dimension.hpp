#pragma once
namespace forge {
// Value-only logical texture shape; usable without cooked data or GPU resources.
enum class TextureDimension { D2, D2Array, Cube, CubeArray, D3 };
} // namespace forge
